# Personal-Linux-Modifications

To use run:
```sh
sudo apt update && sudo apt install git -y  && git clone https://github.com/c3t4r4/Personal-Linux-Modifications.git && cd Personal-Linux-Modifications && ./install.sh
```
